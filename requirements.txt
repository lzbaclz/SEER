# SEER — RTSS 2027 reproducibility manifest.
#
# Install order (assumes Python 3.11 + CUDA 12.x):
#
#   pip install -r requirements.txt
#   pip install --extra-index-url https://pypi.nvidia.com -r requirements-trt.txt
#   pip install -e .
#
# The TensorRT line is split into requirements-trt.txt because it pulls
# from NVIDIA's pip index (~3.5 GB download). All other dependencies
# resolve from the default PyPI mirror.
#
# Versions are pinned to what is verified-working on the reference
# platform (Linux x86_64, CUDA 12.1 driver, 2x A100 SXM4-80GB);
# loosen the lower bounds at your own risk.

# --- Core ML stack ---
torch>=2.2,<2.7
transformers>=4.40,<4.60
datasets>=2.19
accelerate>=0.30

# --- Numerics + data ---
numpy>=1.24,<3.0
pandas>=2.1,<4.0
pyarrow>=15.0
scikit-learn>=1.3

# --- Model export / inference ---
onnx>=1.15
onnxruntime-gpu>=1.17,<2.0   # swap for `onnxruntime` if no GPU
# tensorrt-cu12 — see requirements-trt.txt (separate index)

# --- Training utilities ---
tqdm>=4.66
pyyaml>=6.0
wandb>=0.17  # optional; code falls back to stdout if missing

# --- Metrics ---
rouge-score>=0.1.2
nltk>=3.8

# --- Plotting (figures) ---
matplotlib>=3.8
seaborn>=0.13

# --- Test / lint ---
pytest>=8.0
ruff>=0.4

# --- Inference engine (optional, for full vLLM integration in §6.7) ---
# vllm>=0.5.0
